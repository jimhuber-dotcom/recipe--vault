import { PageHeader } from "@/components/ui/PageHeader";

// Placeholder — replaced with live dashboard queries in the next chunk.
export default function DashboardPage() {
  return (
    <div className="flex flex-col gap-8">
      <PageHeader eyebrow="Your kitchen" title="Dashboard" />
    </div>
  );
}
